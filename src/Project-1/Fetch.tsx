const UserList = () => {
  // fetch user list form https://jsonplaceholder.typicode.com/users

  // NOTE: Render user list with loading and suspend data and control Error handling
  return <>UserList</>;
};

export default UserList;
