const FinallySection = () => {
  

  // NOTE: change response with reduce, map, forEach, filter and find
  // And write custom hook base input
  // Call Previous response - userList
  // Render names or name
  return <></>;
};

export default FinallySection;
